/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pinimg.com",
      "local host",
      "imgs.search.brave.com",
      "miro.medium.com",
    ],
  },
};

export default nextConfig;
